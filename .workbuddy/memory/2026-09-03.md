# 2026-09-03 工作日志

## BGW 面试准备文档
- 在 `F:/Algorithm/面经/` 下新建 `BGW面试准备.md`（提前备战模板），公司暂用代号 BGW（全称待补充）。
- 文档结构：公司背景速览 / 岗位 JD 拆解 / 自我介绍稿 / 项目深挖（TraderAnalysis + test-skills）/ 技术问题清单（7 大类含复选框）/ 反问准备 / 面试记录表 / Action Items。
- 依据用户 QA/测试岗定位与量化背景定制：接口测试 Mock 场景、UI 自动化稳定性、行情业务测试、红涨绿跌等差异化问题已内嵌。

## BGW 面试准备文档（重写为纯问题清单）
- 用户提供 BGW 三轮面经（一面/二面/三面总监面），要求删除此前推论生成的内容，改为「抽象问题 + 留白作答」结构。
- 已重写：按轮次列 Q1..Qn，每题后留「**作答：**」空行；重复问题（Python 高级用法、性能测试、证券项目介绍/问题）在顶部「高频问题」表标注轮次。
- 关键信号：BGW 岗位涉及 web3 业务（三面问「没了解过 web3 怎么适应」）+ 证券项目背景，AI 在测试的应用是考点。用户上一家公司为富途（证券），此岗位为 web3/加密方向。
- 后续调整：用户要求不按一二三面分节，改为三轮整合成单一 Q1~Q15 清单，重复问题用「（X面、Y面·重复）」标注；反问环节合并为一题并列 5 个子点。
- 用户已开始作答：Q1 填「我负责的是esop项目」（esop = 员工持股计划/股权激励系统，富途相关项目）。
- 用户补充了大量面试作答：Q1 技术栈（esop 是 Go 微服务，RPC + REST API）、Q2 OKR 占比（业务70%/接口自动化25%/UI自动化5%）、Q3 自动化产出（P0 100%覆盖，pytest/httprunner/apifox/selenium/ftest）、Q8 性能测试（PHP→GO 重构，TP95<1000ms）、Q10 尾差算法 bug、Q12 AI 用例生成 skill、Q13 web3 适应、Q14 离职原因（八部委政策+发展受限）。
- 已帮用户补全 Q7 Python 高级用法：装饰器/生成器/上下文管理器/魔术方法/协程/GIL/反射+dataclass，附代码示例 + 口述建议 + 高频追问点。
- Q7 按用户要求精简为只保留「装饰器 + 生成器」两个特性，但加深了各自的深度点（装饰器本质/带参两层封装/wraps/叠加顺序；yield 机制/生成器 vs 列表与迭代器/send、yield from），口述建议与追问点同步收窄。原则：求深不求全。
- Q7 示例代码再次简化为最简版（用户嫌 retry/造数例子复杂）：装饰器改为「计算测试用例耗时的 timing 装饰器」（单层闭包），生成器改为「三次方 cube 生成器」。深度点、口述建议、追问点同步改写为与最简例子一致。
- 用户自行把生成器改为「生成器表达式 vs 列表推导式」对比（L1=g / L2=l），我按用户要求检查后修复：① print 补 `:.2f`、去 `end` 缺空格风格问题使注释与输出一致；② 修复 `*3、` 格式错误与 `range(10)` 从 0 起的注释不符（改 range(1,6)），编号统一 2/3；③ 开头总述改「几个 Python 特性」。已用 python 实际运行验证三段示例代码输出正确（0.00s / 1,8,27,64,125 / [1,8,27,64,125]）。
- 用户偏好总结（面试文档迭代）：示例代码要「最简可手写」，拒绝参数化装饰器/多行函数等复杂形态；术语要规范（列表推导式非列表生成式）。
- BGW 文档问题编号重排：用户反映跳号（Q1~Q3 后直接 Q7），已按内容逻辑重排为连续 Q1~Q11（原 Q1~Q15 中 Q4/Q5/Q6 并入 Q3、Q11 并入 Q1，Q9 上一家公司项目缺失已补空题留白）。内容顺序不变，仅标题编号更新。
